# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
*.egg-info/
.eggs/
dist/
build/

# v6 unified data tree (local runtime state)
# Every persistent file lives under ./data/ when JOB_SEARCH_DATA_DIR is unset
# (local dev) or when running the Compose stack with `./data:/data`.
data/

# Legacy dev-mode output dirs (kept for developers running from repo root)
results/*.csv
results/*.xlsx
results/*.json
!results/.gitkeep
logs/
*.log

# User configuration (personal settings)
config/settings.yaml
config/local.yaml
config/*.local.yaml

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
ehthumbs.db

# Temporary files
*.tmp
*.bak
tmp/
temp/

# Jupyter notebooks
.ipynb_checkpoints/
*.ipynb

# Testing
.pytest_cache/
.benchmarks/
.coverage
htmlcov/
.tox/
.nox/

# Type checking and linting
.mypy_cache/
.ruff_cache/

# Docker
.docker/
docker-compose.override.yml

# Environment files (may contain secrets)
.env
.env.*
!.env.example
