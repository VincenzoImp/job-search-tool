# Job Search Tool - Python Dependencies
# Primary dependency metadata now lives in pyproject.toml / uv.lock.
# Keep this compatibility mirror aligned when runtime dependencies change.

# Core job scraping library (requires Python 3.10+)
python-jobspy>=1.1.80,<2.0.0

# Data manipulation and analysis
pandas>=2.0.0,<3.0.0

# Excel file generation with formatting
openpyxl>=3.1.0,<4.0.0

# Configuration management (YAML support)
pyyaml>=6.0.0,<7.0.0

# Retry logic with exponential backoff for rate limiting
tenacity>=8.0.0,<9.0.0

# Interactive dashboard for job results analysis
streamlit>=1.28.0,<2.0.0

# Scheduling for automated periodic execution
APScheduler>=3.10.0,<4.0.0

# Telegram bot notifications
python-telegram-bot>=21.0,<22.0

# Fuzzy string matching for post-filtering
rapidfuzz>=3.0.0,<4.0.0

# Vector search with ChromaDB for semantic job matching
# Uses ChromaDB's built-in ONNX embedder (DefaultEmbeddingFunction, bundling
# all-MiniLM-L6-v2), so no torch / sentence-transformers / transformers needed.
chromadb>=0.4.0,<1.0.0
